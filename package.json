{
  "name": "nickav",
  "version": "0.0.2",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "node scripts/build",
    "deploy": "git subtree push --prefix build origin test-pages",
    "format": "prettier --config package.json --write",
    "format-all": "npm run format $(git ls-files -c -m | grep '\\.\\(jsx\\?\\|scss\\)$' | tr '\n' ' ')"
  },
  "prettier": {
    "singleQuote": true,
    "tabWidth": 2,
    "trailingComma": "es5",
    "arrowParens": "always"
  },
  "devDependencies": {
    "showdown": "^1.9.1"
  }
}
